
// Define the notification reducer
const reducer = (state = null, action = {}) => {
    switch (action.type) {
        default:
            return state;
    }
}

// Export the reducer
export default reducer;