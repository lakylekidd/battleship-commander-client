import React, { Component } from 'react'

export default class CreateJoinContainer extends Component {
  render() {
    return (
      <div>
        <button>New Game</button>
        <button>Join Game</button>
      </div>
    )
  }
}
